class TextValue {
  int sum = 1;
  bool toggle = true;
  bool isFired = false;
  final bool icon;
  final bool con;
  final String textValue;
  final bool backgroundColor;

  TextValue(this.textValue,
      this.icon,
      this.con,
      this.backgroundColor);
}